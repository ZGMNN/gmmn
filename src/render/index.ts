// src/render/ — primitives de dessin p5 (board, checker, dice, modals,
// overlays). Reçoit une instance p5 + l'état courant, dessine, ne mute rien.
//
// Contenu cible : board.ts, checker.ts, dice.ts, modals.ts, overlay.ts,
// chrome.ts (timers/cube/score affichés au-dessus du board).

export {};
