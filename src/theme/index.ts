// src/theme/ — palette, fonts, extraction de la teinte dominante depuis le
// fond aléatoire. Doit pouvoir être appelé avec un id de fond pour produire
// une palette cohérente (board, dés, accents).

export {};
