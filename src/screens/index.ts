// src/screens/ — machines d'état des écrans (intro, sign-in, menu,
// mode-select, lobby, game, profile, about, room).
//
// Chaque screen exporte { setup, draw, mousePressed, keyPressed, cleanup } et
// est sélectionné par main.ts selon l'état global de l'app. Transitions gérées
// par un petit router.ts (à venir).

export {};
