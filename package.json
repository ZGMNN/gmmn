{
  "name": "gmmn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based backgammon game with online multiplayer, player stats, and curated artistic backgrounds.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "firebase": "^10.12.5",
    "p5": "^1.10.0"
  },
  "devDependencies": {
    "@types/p5": "^1.7.6",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0"
  }
}
